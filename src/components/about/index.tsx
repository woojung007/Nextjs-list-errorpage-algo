import styled from "@emotion/styled";

const Wrapper = styled.section`
  padding: 200px 0;
`;

export default function AboutContainer() {
  return (
    <Wrapper>
      <div>about page</div>
    </Wrapper>
  );
}
